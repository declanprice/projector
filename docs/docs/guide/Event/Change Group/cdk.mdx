---
sidebar_position: 2
---

# Cdk

### Usage

```tsx title="lib/event.stack.ts"
import { Stack, StackProps } from 'aws-cdk-lib'
import { Construct } from 'constructs'
import { ChangeGroup } from '@declanprice/projector/cdk'
import { CustomerProjectionHandler } from '../src/customer-change-group.handler'

export class EventStack extends Stack {
    constructor(scope: Construct, id: string, props: EventStackProps) {
        super(scope, id, props)

        new ChangeGroup(this, CustomerProjectionHandler, {
            eventBus: props.eventBus,
            entry: 'src/customer-projection.handler.ts',
        })
    }
}
```

